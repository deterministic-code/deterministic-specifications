version: 1.0.0
includes:
  - view_type_routes:
      filter: inherits == datasource_types
routes:
  - health:
      path: /api/health
      method: GET
      description: Liveness
      response: health
