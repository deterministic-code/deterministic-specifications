expect:
  includes: "duplicate seed id"
  datasource_types: |
    version: CURRENT
    types:
      - user:
          fields:
            - email:
                type: string
                size: 256
---
version: CURRENT
seeds:
  - user:
      - id1:
          email: a@b.c
      - id1:
          email: b@c.d
