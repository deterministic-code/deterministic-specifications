expect:
  covers: additionalProperties:#/$defs/combineOptions
---
version: 1.0.0
services: []
includes:
  - file: x.yaml
    combine_options:
      source:
        remove_services:
          - OldService
      extra: true
