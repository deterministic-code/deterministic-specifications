expect:
  covers: additionalProperties:#/$defs/sideOptions
---
version: CURRENT
services: []
includes:
  - file: x.yaml
    combine_options:
      source:
        remove_services:
          - OldService
        extra: true
