expect:
  covers: additionalProperties:#/$defs/datasourceDef
---
version: CURRENT
datasources:
  - core:
      type: REST
      schema_type: OpenAPI
      schema: https://example.com/o.json
      clients:
        - fetch
      extra: true
