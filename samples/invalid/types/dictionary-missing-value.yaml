expect:
  includes: must contain
---
version: 1.0.0
types:
  - settings:
      inherits: dictionary
      fields:
        - setting_id:
            references: file.id
        - key:
            type: string
            size: 64
