expect:
  covers: required:#/$defs/addFieldOp:add_field
---
version: 1.0.0
types: []
includes:
  - file: x.yaml
    combine_options:
      source:
        remove_types:
          - old
        remove_fields:
          - old.Name
        modify_types:
          - type: old
            new_type: person
        modify_fields:
          - field: old.Name
            new_field: Display
        add_fields:
          - type: old
