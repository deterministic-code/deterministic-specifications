expect:
  covers: type:#/$defs/modifyTypeOp
---
version: 1.0.0
types: []
includes:
  - file: x.yaml
    combine_options:
      source:
        remove_types:
          - old
        remove_fields:
          - old.name
        modify_types:
          - []
        modify_fields:
          - field: old.name
            new_field: display
        add_fields:
          - type: old
            add_field:
              extra:
                type: string
