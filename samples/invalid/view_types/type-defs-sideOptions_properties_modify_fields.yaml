expect:
  covers: type:#/$defs/sideOptions/properties/modify_fields
---
version: 1.0.0
types: []
includes:
  - file: x.yaml
    combine_options:
      source:
        remove_types:
          - old
        remove_fields:
          - old.name
        modify_types:
          - type: old
            new_type: person
        modify_fields: {}
        add_fields:
          - type: old
            add_field:
              extra:
                type: string
