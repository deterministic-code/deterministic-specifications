expect:
  covers: minItems:#/$defs/sideOptions/properties/remove_fields
---
version: CURRENT
types: []
includes:
  - file: x.yaml
    combine_options:
      source:
        remove_types:
          - old
        remove_fields: []
        modify_types:
          - type: old
            new_type: person
        modify_fields:
          - field: old.Name
            new_field: Display
        add_fields:
          - type: old
            add_field:
              Extra:
                type: string
