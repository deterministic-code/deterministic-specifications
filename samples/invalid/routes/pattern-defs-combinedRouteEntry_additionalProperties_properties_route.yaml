expect:
  covers: pattern:#/$defs/combinedRouteEntry/additionalProperties/properties/route
---
version: 1.0.0
routes: []
combined_routes:
  - people:
      route: NOT VALID
      combines:
        - Person
        - reports:
            via: manager
            target: person
            route: /r
