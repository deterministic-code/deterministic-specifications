expect:
  covers: pattern:#/$defs/combinedRouteEntry/additionalProperties/properties/route
---
version: CURRENT
routes: []
combined_routes:
  - people:
      route: NOT VALID
      combined_types:
        - Person
        - reports:
            via: manager
            target: person
            route: /r
