expect:
  covers: pattern:#/$defs/viewTypeRoutesDirective/properties/eager_read_member_only/items
---
version: CURRENT
routes: []
includes:
  - view_type_routes:
      filter: type == "person"
      eager_path:
        - person.reports
      eager_read_member_only:
        - NOT VALID
      eager_write_path:
        - person.reports
