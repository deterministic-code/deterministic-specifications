expect:
  covers: additionalProperties:#/$defs/sideOptions
---
version: CURRENT
routes: []
includes:
  - file: x.yaml
    combine_options:
      source:
        remove_routes:
          - OldRoute
        extra: true
