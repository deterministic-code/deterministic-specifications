expect:
  covers: enum:#/$defs/customRouteShape/properties/authentication
---
version: CURRENT
routes:
  - ping:
      path: /ping
      method: GET
      description: x
      request: person
      response: person
      service: PersonService
      serviceMethod: run
      routeClass: PingRoute
      module: ./x
      authentication: nope
      x-implementation: stub
