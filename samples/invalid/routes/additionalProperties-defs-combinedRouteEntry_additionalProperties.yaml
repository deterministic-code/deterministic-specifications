expect:
  covers: additionalProperties:#/$defs/combinedRouteEntry/additionalProperties
---
version: 1.0.0
routes: []
combined_routes:
  - people:
      route: /api/people
      combines:
        - Person
        - reports:
            via: manager
            target: person
            route: /r
      extra: true
