expect:
  covers: pattern:#/$defs/combinedTypeItem/oneOf/1/additionalProperties/properties/via
---
version: CURRENT
routes: []
combined_routes:
  - people:
      route: /api/people
      combined_types:
        - Person
        - reports:
            via: NOT VALID
            target: person
            route: /r
