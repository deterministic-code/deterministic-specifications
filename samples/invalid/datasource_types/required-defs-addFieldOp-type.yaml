expect:
  covers: required:#/$defs/addFieldOp:type
---
version: CURRENT
types: []
includes:
  - file: other.yaml
    combine_options:
      source:
        add_fields:
          - add_field:
              extra:
                type: integer
