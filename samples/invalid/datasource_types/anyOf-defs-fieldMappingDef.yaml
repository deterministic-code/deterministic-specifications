expect:
  covers: anyOf:#/$defs/fieldMappingDef
---
version: CURRENT
types:
  - user:
      fields:
        - n:
            type: integer
datasource_mappings:
  - user:
      source: users
      field_mappings:
        - n:
            nope: true
