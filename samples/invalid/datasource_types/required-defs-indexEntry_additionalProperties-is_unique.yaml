expect:
  covers: required:#/$defs/indexEntry/additionalProperties:is_unique
---
version: 1.0.0
types:
  - user:
      fields:
        - n:
            type: integer
      indexes:
        - idx:
            fields:
              - n
