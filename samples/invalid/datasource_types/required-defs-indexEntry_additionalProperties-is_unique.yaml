expect:
  covers: required:#/$defs/indexEntry/additionalProperties:is_unique
---
version: CURRENT
types:
  - user:
      fields:
        - n:
            type: integer
      indexes:
        - idx:
            fields:
              - n
