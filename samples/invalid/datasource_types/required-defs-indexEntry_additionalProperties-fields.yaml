expect:
  covers: required:#/$defs/indexEntry/additionalProperties:fields
---
version: 1.0.0
types:
  - user:
      fields:
        - n:
            type: integer
      indexes:
        - idx:
            is_unique: false
