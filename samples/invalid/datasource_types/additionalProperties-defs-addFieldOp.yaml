expect:
  covers: additionalProperties:#/$defs/addFieldOp
---
version: 1.0.0
types: []
includes:
  - file: other.yaml
    combine_options:
      source:
        add_fields:
          - type: user
            add_field:
              extra:
                type: integer
            extra: true
