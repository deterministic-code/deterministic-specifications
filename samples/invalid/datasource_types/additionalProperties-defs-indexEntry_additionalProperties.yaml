expect:
  covers: additionalProperties:#/$defs/indexEntry/additionalProperties
---
version: CURRENT
types:
  - user:
      fields:
        - n:
            type: integer
      indexes:
        - idx:
            fields:
              - n
            is_unique: false
            extra: true
