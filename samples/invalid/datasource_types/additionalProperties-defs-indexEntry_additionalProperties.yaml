expect:
  covers: additionalProperties:#/$defs/indexEntry/additionalProperties
---
version: 1.0.0
types:
  - user:
      fields:
        - n:
            type: integer
      indexes:
        - idx:
            fields:
              - n
            is_unique: false
            extra: true
