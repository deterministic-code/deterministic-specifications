expect:
  covers: additionalProperties:#/$defs/datasourceMappingDef
---
version: CURRENT
types:
  - user:
      fields:
        - n:
            type: integer
datasource_mappings:
  - user:
      source: users
      field_mappings:
        - n:
            source: n
      extra: true
