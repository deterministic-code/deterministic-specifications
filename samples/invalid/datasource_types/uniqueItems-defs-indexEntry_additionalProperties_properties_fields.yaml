expect:
  covers: uniqueItems:#/$defs/indexEntry/additionalProperties/properties/fields
---
version: 1.0.0
types:
  - user:
      fields:
        - n:
            type: integer
      indexes:
        - idx:
            fields:
              - n
              - n
            is_unique: false
